# Normalize line endings in the repository; check out per platform.
* text=auto

# Shell scripts must be LF on disk everywhere or the shebang line breaks.
*.sh text eol=lf

# Binary-ish diagram exports, still text but never worth a CRLF diff.
*.excalidraw text eol=lf
